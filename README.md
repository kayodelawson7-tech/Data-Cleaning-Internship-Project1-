# Data-Cleaning-Internship-Project1-
## Project Overview
The dataset was examined for missing values, duplicate records, inconsistent formatting, incorrect data types. Missing values were identified only in the CouponCode field and were replaced with "No Coupon" because they represented transactions where no promotional code was used. No duplicate records were found. The Date column was converted from text to a date format to facilitate time-series analysis.
